---
- name: install various packages
  apt:
    name: "{{ item }}"
    state: present
  with_items:
    - curl
    - expect
    - git
    - htop
    - lm-sensors
    - sysstat
    - unzip
    - vim
    - virtualbox
    - qemu
